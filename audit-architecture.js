const fs=require('fs'),p=require('path');const files=fs.readdirSync('.').filter(f=>f.endsWith('.html'));const set=new Set(files);let missing=[],inbound={};files.forEach(f=>inbound[f]=0);for(const f of files){const s=fs.readFileSync(f,'utf8');for(const m of s.matchAll(/href="([^"]+)"/g)){let h=m[1].split('#')[0].split('?')[0];if(!h||h.startsWith('http')||h.startsWith('mailto:')||h==='./')continue;if(h.endsWith('/'))h+='index.html';if(!set.has(h)){if(h.endsWith('.html'))missing.push(`${f} -> ${h}`)}else inbound[h]++;}}
const orphans=files.filter(f=>f!=='index.html'&&f!=='404.html'&&inbound[f]===0);console.log('HTML='+files.length);console.log('MISSING='+missing.length);missing.slice(0,30).forEach(x=>console.log(x));console.log('ORPHANS='+orphans.length);orphans.forEach(x=>console.log('ORPHAN '+x));const base='https://bouhan.rss7.net/';fs.writeFileSync('sitemap.xml','<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n'+files.filter(f=>f!=='404.html').map(f=>`  <url><loc>${f==='index.html'?base:base+f}</loc></url>`).join('\n')+'\n</urlset>');